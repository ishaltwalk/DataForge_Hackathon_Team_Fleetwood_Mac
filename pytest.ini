[pytest]
# Scope collection to tests/. Without this, bare `pytest` also walks rime_tts/
# and scripts/, where the manual_check_* files hit the live Rime API on import
# and cost money to "collect". The README tells judges to run `pytest`, so it
# has to mean the offline suite and nothing else.
testpaths = tests
addopts = -q
